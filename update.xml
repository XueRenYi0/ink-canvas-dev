<?xml version="1.0" encoding="UTF-8"?>
<!-- AutoUpdater.NET version description file (fetched on startup, compared with local version) -->
<!-- Release steps: update version + url + changelog, then push to master -->
<!-- IMPORTANT: this file MUST stay pure ASCII (bytes 0-127 only), comments AND changelog included.
     Reason: AutoUpdater.NET downloads it with WebClient.DownloadString without setting Encoding,
     which decodes with the system ANSI codepage. Any non-ASCII byte then turns into mojibake that
     breaks the XML structure and the whole update check fails with "XML document has an error".
     So the changelog points to the release page (Chinese notes live there) instead of inline text.
     Verify before pushing: every byte < 128, and [xml]$s parses fine. -->
<item>
    <version>6.3.3</version>
    <url>https://gh-proxy.com/https://github.com/XueRenYi0/ink-canvas-dev/releases/download/v6.3.3/Inkboard-v6.3.3-Setup.exe</url>
    <changelog>https://github.com/XueRenYi0/ink-canvas-dev/releases/tag/v6.3.3</changelog>
    <mandatory mode="2">false</mandatory>
</item>
