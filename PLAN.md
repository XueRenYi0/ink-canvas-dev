# 极简批注软件开发计划

> 目标：一款极简、美观、轻量的 Windows 屏幕批注软件（悬浮工具栏 + 全屏批注层），适配 4GB 内存老机器，面向讲课演示与日常办公批注。
> 本文档为总计划，各阶段完成后在此对照勾选。

---

## 1. 项目概述

| 项 | 内容 |
|---|---|
| 定位 | 极简批注工具（对标 gInk / Snipaste，差异化：矢量笔迹 + 笔迹回放 + 手写表达式识别） |
| 平台 | Windows 10 / 11（单显示器，需高 DPI 适配） |
| 硬件目标 | 4GB 内存可流畅运行 |
| 交互形态 | 系统托盘常驻 + 全局热键唤起 + 悬浮工具栏（dock 式，极简） |
| 与本仓库 Ink Canvas 的关系 | 本仓库（Ink Canvas）为功能繁多的教学软件，本计划为**全新极简项目**；Ink Canvas 的 MathGraph 模块作为手写表达式识别的参考实现 |

## 2. 技术选型（定稿）

| 层 | 选型 | 说明 |
|---|---|---|
| 框架 | **Tauri 2.0**（Rust + WebView2） | 体积小、内存低（典型 40–100MB），不打包 Chromium |
| 前端 | **Svelte 5 或 Vanilla TS**（不引入 React/Vue） | 轻量；不使用 Konva/Fabric 等画布框架，对象模型自研 |
| 笔迹 | **perfect-freehand**（MIT） | 压感手绘线条，tldraw 同款 |
| 截屏 | **xcap**（Rust crate，Apache-2.0） | `screenshots` crate 官方继任者，支持区域捕获 |
| 全局热键 | tauri-plugin-global-shortcut（官方） | |
| 剪贴板 | tauri-plugin-clipboard-manager（官方） | |
| 公式渲染（后期） | KaTeX / MathLive | 手写表达式识别结果的规整渲染 |
| 系统托盘 | Tauri 内置 tray | |

## 3. 系统架构

```
┌─ 前端 (TS + Canvas)
│   ├─ 画布分层：底图层(截图/白板底色) / 笔迹层(矢量对象) / 活动层(正在绘制的笔画,低延迟) / UI层(工具栏)
│   ├─ 对象模型：Stroke{类型/颜色/粗细/点集(含压感+时间戳)/包围框} —— 对象级存储
│   ├─ 坐标系统：世界坐标 + 视口变换（支撑无限白板滚动/缩放；单屏，无多屏映射层）
│   └─ 统一输入管线：Pointer Events（pen/touch/mouse 三合一）
├─ Tauri 窗口层
│   ├─ 全屏透明无边框窗口 + alwaysOnTop
│   ├─ setIgnoreCursorEvents → 鼠标穿透模式
│   └─ 悬浮工具栏窗口（可拖动、自动隐藏）
└─ Rust 侧
    ├─ 截屏(xcap) / 剪贴板 / 托盘 / 全局热键
    └─ 后期：手写表达式识别推理（见 §5）
```

**关键设计决策（P1 落地，后期不可轻易改动）：**
1. 笔迹按"对象"存储而非位图 → 天然支持对象级橡皮、撤销重做、选中编辑、回放
2. 世界坐标 + 视口变换 → 白板无限滚动、缩放、回放共用一套坐标
3. 每个点记录时间戳 → 笔迹回放几乎零成本
4. 每个对象存储包围框 → P2 命中检测的基础
5. 活动层 canvas 用 `{ desynchronized: true }` 上下文 → 降低书写延迟 30–50ms

**输入管线（P1 一次性做好）：**

| 输入 | 行为 |
|---|---|
| pen（数位笔） | `e.pressure` → perfect-freehand 变宽；`getCoalescedEvents()` 补齐 240Hz 采样；笔侧键=临时橡皮；笔尾倒置=橡皮；预留 tiltX/tiltY 笔锋 |
| touch | 单指=画笔（可关）；双指=平移画布；捏合=缩放；`touch-action:none` 禁默认手势；笔在感应范围时忽略 touch（防手掌误触） |
| mouse | 正常绘制；pressure=0.5 恒定，按速度模拟粗细降级 |

## 4. 功能清单

### 笔迹工具
- [ ] 圆珠笔（压感 + perfect-freehand，颜色/粗细可调）
- [ ] 荧光笔（半透明，multiply 混合不遮字）
- [ ] 橡皮擦（对象级整体擦除）
- [ ] 文字工具
- [ ] 马赛克/模糊（截图隐私场景）

### 图形工具
- [ ] 直线、箭头、矩形、椭圆（含描边/填充）
- [ ] 图形控制点（拖拽调整大小/端点）
- [ ] **手写表达式识别（见 §5 专项设计）**

### 编辑
- [ ] 撤销 / 重做（Ctrl+Z / Ctrl+Shift+Z）
- [ ] 选择工具：点选 / 框选 → 拖动、删除(Del)、复制粘贴(Ctrl+C/V)、选中后改笔（换色/换粗细）
- [ ] 笔迹回放（P 键；逐点按时间轴重绘，0.5×/1×/2× 变速、暂停、进度条）

### 画布与输出
- [ ] 白板模式（无限滚动：滚轮/双指平移、缩放；底色白/黑/自定义）
- [ ] 截图模式（区域框选 → 截屏作底图 → 进入批注；F1）
- [ ] 保存 PNG（Ctrl+S）、一键复制剪贴板（Ctrl+C 无选中时）
- [ ] 钉图 Pin（批注结果固化为可拖动/缩放悬浮窗）

### 系统
- [ ] 系统托盘常驻、开机自启（可选）
- [ ] 全局快捷键（见 §7，支持自定义）
- [ ] 鼠标穿透模式（批注中临时操作底层软件再返回）
- [ ] 预设笔（1/2/3 数字键快速切换 颜色+粗细+透明度 组合）
- [ ] 设置界面（主题/默认笔/快捷键自定义）

### 明确不做
- 多显示器支持（已确认砍掉，坐标系统仍按世界坐标设计不损失扩展性）
- 滚动长截图自动拼接（P6 后再议）

## 5. 手写表达式识别专项设计（初高中）★后期图形工具扩展

### 5.1 功能描述
在图形工具中新增"表达式"工具：框选手写内容 → 识别 → 生成规整图形，两类产出：
1. **规整公式**：手写 `½x²+1` → 印刷体公式对象（可缩放、可编辑）
2. **函数图像**：手写 `y=x²-2x+1` → 自动建坐标系并绘制抛物线（初高中函数教学核心场景）

### 5.2 识别范围（初高中数学）
- 代数式 / 方程 / 不等式（含分式、根号、指数、绝对值）
- 函数：一次、二次、反比例、幂/指/对数、三角函数
- 圆锥曲线：圆、椭圆、双曲线、抛物线（标准方程）
- 不追求：矩阵、微积分、化学式（超出初高中）

### 5.3 技术路线（二选一，P6 前做技术验证）
| 路线 | 方案 | 优缺点 |
|---|---|---|
| A. 微软原生（推荐优先验证） | Rust 侧调 Windows **Math Input Control / Ink Analysis API**（COM/WinRT，Windows 内置免费），输出 MathML | 免费本地、Windows 集成好；仅限 Windows；**本仓库 Ink Canvas 的 MathGraph 模块即是此路线的成熟参考**（Interop.micaut.dll → MathMLParser → GraphBuilder，算法可直接移植） |
| B. 开源/在线 | Pix2Text ONNX 本地推理，或 Mathpix API（在线） | 跨平台/精度高；模型体积大或需付费联网 |

### 5.4 渲染与绘图
- 规整公式：KaTeX / MathLive 渲染为对象
- 函数图像：MathML/表达式 → 自研轻量解析器 → Canvas 坐标系绘制（参考本仓库 `GraphBuilder.cs` 的取值/描点逻辑）

### 5.5 交互流（设计原则：识别可修正，不做"黑盒一步到位"）
```
框选手写笔迹 → 识别面板(展示识别结果+候选修正) → [插入公式] 或 [绘制图像]
                                                    ↘ 识别错误 → 重新手写/放弃
```

### 5.6 阶段归属
放 **P6**。前置条件：P2 选择工具（框选笔迹能力）已就绪。

## 6. 分阶段计划

| 阶段 | 内容 | 产出 / 验收标准 | 状态 |
|---|---|---|---|
| **P1 核心画板** | 透明覆盖窗口、统一输入管线(pen/touch/mouse)、圆珠笔(压感+coalesced+desynchronized 低延迟)、荧光笔、对象级橡皮、笔侧键/笔尾橡皮、防误触、撤销重做、悬浮工具栏、世界坐标+视口、时间戳记录 | 能画能擦；数位笔书写延迟肉眼无感知；Wacom/Huion/Surface 真机通过 | ☐ |
| **P2 矢量编辑** | 选择工具(点选/框选/拖动/删除/复制粘贴/改笔)、直线/箭头/矩形/椭圆+控制点、文字工具、马赛克 | 完整编辑能力，命中检测准确 | ☐ |
| **P3 截图输出** | 区域框选截屏(xcap)、保存 PNG、复制剪贴板、全局热键(F1)、系统托盘、鼠标穿透模式 | **v0.1 可发布**：完整"截图→批注→保存/复制"闭环 | ☐ |
| **P4 白板回放** | 无限滚动白板(滚轮/双指平移、捏合缩放)、笔迹回放(变速/进度条)、预设笔+数字键切换 | 讲课场景完整可用 | ☐ |
| **P5 打磨** | 钉图(拖动/缩放/阴影)、设置界面(快捷键/主题/默认笔)、取色器 | v1.0 | ☐ |
| **P6 进阶** | **手写表达式识别(§5)**、录屏/GIF、OCR | 差异化卖点落地 | ☐ |

## 7. 快捷键方案（接轨主流，设置页可自定义）

| 快捷键 | 功能 | 参照 |
|---|---|---|
| F1 | 截图批注 | Snipaste |
| B / H / E | 圆珠笔 / 荧光笔 / 橡皮 | OneNote |
| S / T / M | 选择 / 文字 / 马赛克 | — |
| R / L / A / O | 矩形 / 直线 / 箭头 / 椭圆 | 造型直觉 |
| 1 / 2 / 3 | 预设笔切换 | gInk/Snipaste |
| X | 手写表达式识别（P6） | — |
| Space 按住 | 临时平移画布 | Figma |
| P | 笔迹回放 | 自有 |
| Ctrl+Z / Ctrl+Shift+Z | 撤销 / 重做 | 通用 |
| Ctrl+C / Ctrl+V | 复制 / 粘贴（无选中时 Ctrl+C=复制整图） | 通用 |
| Ctrl+S | 保存 PNG | 通用 |
| Del | 删除选中 | 通用 |
| Esc | 退出当前模式 | 通用 |

## 8. 依赖库与许可

| 库 | 用途 | 许可 |
|---|---|---|
| perfect-freehand | 压感笔迹生成 | MIT |
| xcap | 屏幕捕获 | Apache-2.0 |
| tauri-plugin-global-shortcut | 全局热键 | MIT/Apache |
| tauri-plugin-clipboard-manager | 剪贴板 | MIT/Apache |
| KaTeX / MathLive（P6） | 公式渲染 | MIT |
| Windows Math Input Control（P6 路线 A） | 手写公式识别 | Windows 内置 |

全部许可可商用。

## 9. 参考项目

| 项目 | 参考价值 |
|---|---|
| **本仓库 Ink Canvas / MathGraph 模块** | 手写表达式→函数图像的现成实现（Math Input Control → MathMLParser → GraphBuilder），算法与产品形态直接参考 |
| gInk（C#, MIT） | 最接近的产品定位；"预设笔"交互（每支笔=颜色+粗细+透明度组合）；压感/触摸/穿透的兼容性经验；其多显示器 DPI 坑 ours 已规避 |
| ppInk（gInk fork） | 图形/文字工具在 gInk 架构上的扩展方式 |
| tldraw（TS） | 选择工具、包围框、控制点、无限画布、命中检测的算法参考（不引入其代码，抄思路） |
| Excalidraw（TS） | 双指平移/捏合缩放手势实现 |
| Snipaste / PixPin（闭源） | 钉图、截图交互的产品标杆 |

## 10. 风险与待验证项

- [ ] 数位板兼容清单：Wacom(新驱动默认 Windows Ink) / Huion / XP-Pen(驱动需开 Ink 模式) / Surface 触屏笔 / 纯鼠标降级 —— P1 真机验收
- [ ] WebView2 压感事件完整性与 `getCoalescedEvents` 在 WebView2 中的行为
- [ ] 点击穿透与"切回"交互闭环（穿透后仅热键/托盘可召回）
- [ ] Windows 10 无 WebView2 Runtime 的安装引导（下载引导程序约 1.8MB）
- [ ] 高 DPI（150% 缩放）下笔迹与光标零偏移
- [ ] Math Input Control 在 Tauri(Rust) 中调用的可行性验证（P6 前技术预研）
- [ ] 4GB 内存机器实测（目标：空闲驻留 <100MB）

## 11. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-05 | 初版：P1–P6 计划定稿；确认需求——单显示器、笔迹选中、笔迹回放、主流快捷键、手写板/触摸屏手势；新增 §5 手写表达式识别专项设计（初高中） |
