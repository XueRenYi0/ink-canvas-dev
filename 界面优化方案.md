# Ink Canvas 界面优化方案 v3.1 · 把手 + 抽屉 + 上下文菜单

> **定位**：学校大屏为主战场，兼顾手写板与家用电脑。
> **核心理念**：屏幕上只有一个可拖动的把手，教学内容零遮挡；点开把手是全功能大按钮抽屉；**选中工具后，参数面板贴着工具弹出（上下文菜单）**。无档位、无向导、无新概念——老师需要理解的东西只有两句：「点把手，找工具」「改什么，点什么」。
> **基线**：基于 v5.2.0 代码结构（`Ink Canvas/`，MainWindow 已拆分为 20+ 分部类）。

---

## 0. v2 → v3 决策记录

| v2 设计 | 结论 | 原因 |
|---|---|---|
| 三档渐进式（极简/标准/专业） | **砍掉** | "档位"本身就是学习成本；与"零概念"目标矛盾 |
| 首次启动三选一向导 | **砍掉** | 又一个流程；抽屉方案不需要预先选档 |
| 数学盘藏在档位 2 | **改** | 数学工具与图形同区，开抽屉一步直达 |
| 三档 × 三输入 9 种组合 | **简化** | 输入自适应压缩为一个 `IsTouchMode` 开关 |
| 荧光笔 / 放大镜 / 遮罩 / 量角器 | **新增** | v2 漏掉的课堂刚需（竞品全有，本项目全无） |

**v3.1 增补**（融合版本 B 上下文菜单）：

| 增补 | 说明 |
|---|---|
| 双击把手 = 开写 + 迷你设置面板 | 颜色 / 粗细 / 荧光笔贴着把手弹出，改完点空白处即收——最高频路径两秒内完成 |
| 抽屉选工具 → 上下文面板顶上 | 点「图形」抽屉即收，图形 + 数学面板在同一位置出现；选中具体图形后面板收起、直接开画 |
| 点橡皮 → 擦除方式面板 | 笔画擦除 / 区域擦除 / 清屏三选，就地切换 |
| 遮罩幕布升级为双模式 | 聚光灯（拖洞）+ 擦除蒙层（刮刮卡），详见 §4.1 |

---

## 1. 用户画像

| 画像 | 场景 | 在 C 方案下的体验 |
|---|---|---|
| 新手 / 代课老师 | 被临时拉来用大屏 | 屏幕上就一个圆把手，第一次还有气泡提示「点我」；抽屉里图标大、分区清，不会点错 |
| 数学老师 | 函数、几何、公开课 | 抽屉「图形与数学」区：图形 / ƒx 函数 / 量角器 / 直尺 / 圆规一步直达 |
| 日常授课老师 | PPT + 板书 | 双击把手 = 画笔直接开写；PPT 放映时把手自动浮现 |
| 家用备课 | 鼠标 + 键盘 | 抽屉同样可用，全量快捷键照常工作 |

---

## 2. 把手（唯一常驻元素）

### 2.1 外观与位置

- 56px 圆形按钮（大屏触摸模式），笔形图标，深色底
- 默认停在**右侧边缘垂直居中**；可拖到任意位置
- **贴边自动缩为一半**，不挡内容；离开边缘恢复
- PPT 开始放映 → 把手自动浮现；退出放映 → 自动隐藏（沿用现有 PowerPoint 联动）

### 2.2 交互

| 手势 | 动作 | 说明 |
|---|---|---|
| 单击 | 打开 / 收起抽屉 | 唯一的主入口 |
| 双击 | 画笔模式开 / 关 **+ 迷你设置面板** | 「拿笔就写」；颜色 / 粗细 / 荧光笔贴着把手弹出，点空白处即收 |
| 拖动 | 移动位置 | 拖到边缘自动吸附半隐藏 |
| 长按（触摸）/ 右键 | 快捷菜单 | 设置、重启、退出、隐藏 |

### 2.3 新手引导

- 首次运行：把手旁气泡「点我选工具 · 双击直接用笔写字」，点击任意处消失
- 不做向导、不做问卷、不做默认档位——零前置流程

---

## 3. 抽屉（全功能面板）

### 3.1 结构

从把手所在边缘展开（右侧为例），分区卡片，44–56px 大按钮：

```
┌ 工具 ──────────────────── ✕ ┐
│ 书写                        │
│ [✏ 画笔] [🖍 荧光笔] [◻ 橡皮] [▭ 选择] │
│ 图形与数学                   │
│ [◯ 图形] [ƒx 函数] [∠ 量角器] [⌖ 放大镜] │
│ 页面                        │
│ [◀] [2/3] [▶] [＋ 加页]      │
│ 课堂工具                     │
│ [▤ 遮罩] [⏱ 计时] [⚄ 点名] [📷 截图] │
│ 设置 · 保存 · 更多            │
└────────────────────────────┘
```

### 3.2 关键交互规则（上下文菜单核心）

- **选完即收**：点了笔 / 橡皮 / 图形等模式类工具，抽屉自动收起——不打断讲课节奏
- **参数面板顶上**：抽屉收起的同时，该工具的上下文面板在同一位置出现：
  - 画笔 / 荧光笔 → 颜色环 + 粗细三点
  - 橡皮 → 笔画 / 区域 / 清屏三选
  - 图形 → 基本图形网格 + 坐标系 + ƒx 函数 + 图形库入口
- **面板的消失时机**：选中具体图形（如椭圆）→ 面板收起、直接开画；或点击面板外任意空白处
- **换工具 = 面板内容自动切换**，不需要先关再开
- 荧光笔是独立按钮（不是笔的附属选项），选中即切换到半透明高亮笔
- 抽屉 / 面板打开时点击外部区域 = 收起，不误触

### 3.3 工具迁移清单（现有功能 → 抽屉位置）

| 现有功能 | 抽屉位置 |
|---|---|
| 画笔 / 色板 / 粗细 / 橡皮 / 选择 | 书写 |
| 图形面板 / 我的图形库 / 坐标系图形 / 函数作图 | 图形与数学 |
| 加页 / 翻页 / 页码 / 白板开关 | 页面 |
| 倒计时 / 随机点名 / 截图 / 保存墨迹 / 墨迹回放 | 课堂工具 |
| 多人书写 / 白板底纹 / 双指手势锁 / 设置 / 重启 / 退出 | 更多（或设置页） |

旧悬浮条整条退役，由把手替代——20 个平铺按钮的时代结束。

---

## 4. 新增功能（四件课堂刚需）

| 功能 | 说明 | 实现要点 |
|---|---|---|
| **荧光笔** | 半透明高亮笔，压在字上不遮挡 | WPF `DrawingAttributes.IsHighlighter = true`，成本极低 |
| **遮罩幕布** | 双模式：聚光灯 + 擦除蒙层（详见 §4.1） | 独立 `AllowsTransparency` 窗口 |
| **放大镜** | 框选区域放大悬浮显示，看小字 / 学生作业细节 | `VisualBrush` 缩放渲染或调用系统放大镜；双击退出 |
| **量角器 / 直尺 / 圆规** | 屏幕虚拟教具：可拖动、可旋转、透明 | 透明置顶窗口 + `RotateTransform`；笔尖沿边缘吸附（`StylusPlugIns` 层做命中偏移） |

数学工具不单独成盘，与图形同区——数学老师开抽屉一步直达。

### 4.1 遮罩幕布 · 详细交互设计

**入口**：抽屉「课堂工具」区 + 快捷键 M。开启后把手转为半透明但保持可见——保证任何时候都能退出，不会把老师"锁死"在幕布里。

**模式一 · 聚光灯**（讲到哪照到哪）

- 全屏 75–80% 黑幕，中间一个透明洞（默认约 300×200）
- **单指拖动洞口**移动，逐步揭示内容——像舞台追光
- 洞口四角手柄 / 双指捏合缩放大小
- 右下角迷你控制条：模式切换 · 重蒙 · 退出

**模式二 · 擦除蒙层**（刮刮卡，互动神器）

- 全屏蒙层盖住答案，**手指或笔擦到哪里哪里透**
- 擦除笔刷三档大小可调
- **一键重蒙**：擦完想再来一遍（下一节课 / 另一个班）点一下即恢复全覆盖
- **一键全开**：全部揭开直接讲解
- 课堂用法：请学生上台亲手擦出答案，比老师点一下出现更有仪式感（希沃蒙层同款玩法）

**通用规则**

- 幕布之下仍可批注：墨迹画在幕布**上层**，遮罩 + 批注可叠加使用
- ESC / 控制条 ✕ 退出，幕布记忆上次模式
- 实现要点：全屏 `WindowStyle=None + AllowsTransparency` 黑幕窗口；聚光灯透明洞用 `CombinedGeometry`（`Exclude`）挖出；擦除蒙层用一层 `InkCanvas`（透明笔刷擦除 `EraserShape` 大笔头）或位图 alpha 涂抹，擦除轨迹即洞

---

## 5. 输入方式自适应（简化版）

自动检测，一个布尔搞定 90% 差异：

| 参数 | 触摸模式 | 鼠标模式 |
|---|---|---|
| 把手尺寸 | 56px | 40px |
| 抽屉按钮 | 48–56px | 36px |
| 长按提示 | ✓（600ms） | 悬停 tooltip（300ms） |
| 悬停态 | 禁用（红外屏无悬停） | ✓ |
| 手掌抑制 | 强制开启 | 关闭 |

- 检测：启动时 `Tablet.TabletDevices`；运行时跟踪最近输入设备，**10 秒防抖**后切换
- 设置 → 界面 → 输入方式：自动 / 触摸 / 鼠标（手动覆盖）
- 快捷键全环境保留（家用电脑重度依赖），触摸模式不展示快捷键提示

---

## 6. 视觉规范

- **图标**：全量切换 `Resources/Icons/Fluent`（60+ 张已就位），废弃 MDL2 / 位图混用
- **主题 token**：`Dark.xaml` / `Light.xaml` 扩充：
  - `HandleBackground / DrawerBackground / DrawerButtonFill / DrawerButtonStroke`
  - `AccentSelected`（收编 `#FF0088FF` 选中环）、`DangerBadge`（收编 `#E5484D`）、`SurfaceConfirm`
- **三态反馈**：悬停 / 按下 / 选中，抽屉按钮必须有明确选中态（当前工具在抽屉里高亮显示）
- **弹层根治**：抽屉用独立定位面板实现，摆脱 Viewbox 内无法用 `Popup` 的限制（负 Margin 悬挂全部废除）

---

## 7. 实施路线图

| 阶段 | 内容 | 改动面 | 风险 |
|---|---|---|---|
| **Phase 1** | 视觉统一：Fluent 图标全量、token 补全、44px+ 触控目标、三态 | 资源层 + XAML | 极低 |
| **Phase 2** | 把手 + 抽屉骨架：现有工具全部迁入抽屉，旧悬浮条退役 | XAML 重排 + 少量代码 | 低 |
| **Phase 3** | 新功能：遮罩幕布（优先，双模式）→ 荧光笔 → 放大镜 → 虚拟教具 | 新增窗口 / 功能 | 中 |
| **Phase 4** | 输入自适应 + 设置页翻新（iNKORE 控件重排）+ 旧 `-o` UI 退役 | 配置 + 设置页 | 低 |

每阶段独立发版；Phase 1 + 2 可合并先行。

---

## 8. 技术实现要点（WPF）

- **把手**：复用现有悬浮条的拖动逻辑（`MW_FloatBar.cs`），收成单按钮；贴边吸附监听位置阈值
- **抽屉**：非 `Popup`、非 Viewbox 子元素——用窗口内独立 `Canvas` 定位面板，随把手位置计算展开方向（右缘→左开，下缘→上开），避免被裁剪
- **双击把手**：`DispatcherTimer` 300ms 内二次点击判定，切换画笔 / 鼠标模式（复用现有模式切换代码）；开写同时弹出迷你设置面板（颜色 / 粗细 / 荧光笔），点空白即收
- **上下文面板**：与抽屉同一定位机制（窗口内独立 `Canvas`，随把手位置计算展开方向）；一个面板控件按当前工具切换内容模板（`ContentTemplateSelector`），换工具不换面板位置——肌肉记忆不中断
- **选完即收**：工具按钮 `Click` 里统一 `CloseDrawer()` 再执行原 handler——所有现有事件处理器零改动
- **荧光笔**：`DrawingAttributes.IsHighlighter`；颜色默认黄，粗细固定宽头
- **遮罩**：全屏 `WindowStyle=None` + `AllowsTransparency` 窗口；聚光洞 = `CombinedGeometry` Exclude 挖洞，拖动改 `Geometry` 参数；擦除蒙层 = 顶层 `InkCanvas` 白色不透明笔迹 + `PointEraserShape` 大笔头擦除（擦除轨迹即透明洞），重蒙 = 清空该层重铺；ESC 全局钩子退出
- **放大镜**：`VisualBrush` 绑定屏幕 `Visual`，`Transform` 缩放；圆框窗口
- **虚拟教具**：透明 PNG 教具窗口 + 旋转手柄；吸附在 `StylusPlugIns.DynamicRenderer` 层做（墨迹落点向教具边缘投影）
- **配置**：`IsTouchMode`、把手位置、抽屉分区开关写入现有 Settings 体系；按机器记忆

---

## 9. 参考产品与借鉴点

| 产品 | 借鉴 | 避坑 |
|---|---|---|
| Epic Pen | 极简哲学 "stays out of your way"；可收起 / 贴边工具条 | — |
| ZoomIt | 放大镜是课堂看细节的杀手锏；双击直达高频动作 | 快捷键驱动不可照搬到无键盘大屏 |
| 希沃白板 5 | 遮罩蒙层、学科工具、大按钮面板 | 工具藏太深、入口难找——C 方案抽屉一步全展开 |
| GoodNotes 6 | Active Tool Menu：参数贴着工具弹（本项目 §3.2 同款思路） | 2025-09 过度收纳遭 459 票投诉——高频工具（笔）用双击把手兜底 |
| Microsoft Whiteboard | Fluent 圆角、大触控目标、清晰选中态 | — |

---

## 附：现状问题清单（v1 诊断，Phase 1 解决）

1. **单排过载**：白板模式悬浮条约 20 项平铺一排，无分组分隔
2. **触控目标偏小**：按钮 36px 级，86″ 红外屏误触率高（规范建议 ≥44px）
3. **图标三套体系并存**：MDL2 / 自绘 Path / 位图混用，Fluent 库已就位未启用
4. **主题 token 匮乏**：仅 5 个画刷，硬编码色散落各处
5. **弹层脆弱**：Viewbox 内不能用 Popup，面板全靠负 Margin 悬挂，位置写死
