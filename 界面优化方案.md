# Ink Canvas 界面优化方案 v2 · 渐进式界面

> **定位**：学校大屏为主战场，兼顾手写板与家用电脑三种环境。
> **目标**：让"啥也不懂的老师"零学习成本上手批注，让数学老师工具齐全，让所有功能在需要时才出现。
> **基线**：基于 v5.2.0 代码结构（`Ink Canvas/`，MainWindow 已拆分为 20+ 分部类）。

---

## 1. 核心设计原则

| 原则 | 说明 | 反面案例 |
|---|---|---|
| 渐进式披露 | 初始只给 5–6 个最常用工具，复杂功能按需展开 | 希沃白板：功能全，但"批注在哪"是教师高频求助问题 |
| 高频优先 | 任何收纳不得牺牲 笔 / 橡皮 / 撤销 一键直达 | GoodNotes 2025-09 把笔/橡皮收进单图标，遭 459 票投诉回滚 |
| 状态可见 | 开关两态必须一眼可辨 | 本项目白板图标的红色 ✕ 角标已是正面案例，延续此思路 |
| 环境自适应 | 大屏 / 手写板 / 鼠标自动切换交互参数，不强迫用户配置 | — |

---

## 2. 用户画像与场景

### 2.1 三类教师

| 画像 | 特征 | 核心诉求 | 对应档位 |
|---|---|---|---|
| 新手老师 | 被临时拉来用大屏、代课老师、临近退休不碰电脑的老师 | 打开就能写字，5 分钟学会，怕点错按钮 | 档位 0 |
| 日常授课老师 | 每天用 PPT + 板书 | 颜色粗细顺手，图形 / 截图 / 白板够用 | 档位 1 |
| 数学 / 重度老师 | 函数、几何、多页板书、公开课 | 坐标系、函数图像、图形库、多人书写、缩略图 | 档位 2 |

### 2.2 三种使用环境

| 环境 | 输入 | 特点 |
|---|---|---|
| 学校大屏（主战场） | 触摸 + 教鞭笔 | 86″ 红外屏、远距离操作、误触率高、无悬停、讲台无键盘 |
| 手写板 + 电脑 | 数位板 / 数位屏 + 鼠标 | 办公室备课批注、精度高、有悬停 |
| 家用电脑 | 纯鼠标 | 备课写教案、看回放、高度依赖快捷键 |

---

## 3. 三档渐进式界面（核心方案）

### 3.1 档位 0 · 极简批注（出厂默认）

**6 个按钮封顶，56px 超大触控目标。**

- 画笔（当前颜色常显在按钮上，点击即写）
- 橡皮
- 撤销
- 翻页组（仅 PPT 放映时出现）
- 收起（回到鼠标模式，不退出程序）
- 右端「≫」= 展开更多（唯一的升级入口，不做任何隐藏手势）

规则：

- PPT 开始放映 → 自动切入档位 0 并显示（延续现有 PPT 联动行为）
- 首次使用弹出气泡引导：「直接在屏幕上写字，写错点撤销，完事点收起」
- 无二级面板、无长按依赖、无右键依赖
- 设计哲学参考 Epic Pen 的 "overlay stays out of your way"

### 3.2 档位 1 · 标准教学

在档位 0 基础上补齐日常授课（约 15 项，分组排布）：

| 分组 | 内容 |
|---|---|
| 书写组 | 画笔 + 6 色板 + 粗细三档 |
| 修正组 | 橡皮、选择、撤销、重做 |
| 页面组 | 清屏（二次确认）、截图、白板/黑板、翻页/页码 |
| 尾部 | 「⋯」收纳：设置、重启、退出 |

### 3.3 档位 2 · 专业全能

在档位 1 基础上再加（约 22 项）：

- **数学工具盘**（见 §4）
- 我的图形库（已有功能，入口化）
- 多人书写、双指手势锁、白板底纹（已有，收纳进此档）
- 多页缩略图侧栏
- 课堂工具：倒计时、随机点名（`CountdownTimerWindow` / `RandWindow` 已有）

### 3.4 档位切换机制

| 操作 | 效果 |
|---|---|
| 点右端「≫」 | 升一档（150ms 平滑伸展动画） |
| 点左端「≪」 | 降一档 |
| 双击把手 | 直接回档位 0 |
| 右键把手菜单 | 三个档位直接选择 |
| 设置页 | 界面 → 工具档位 |

- **档位按设备记忆**：教室大屏和家里电脑各自记各自的（配置 key 带机器标识）
- 降档只隐藏不销毁控件，热路径零延迟

### 3.5 首次启动向导（三选一）

复用 `WelcomeWindow.xaml` 扩展成三张卡片：

1. 「第一次用 / 给代课老师」→ 档位 0 + 气泡引导
2. 「日常上课」→ 档位 1
3. 「数学老师 / 重度用户」→ 档位 2 + 数学盘常驻

可跳过（默认选 2）；选择结果随时可在设置里改。

---

## 4. 数学工具盘（档位 2 专属）

| 工具 | 说明 | 复用现状 |
|---|---|---|
| 虚拟直尺 / 三角板 / 量角器 / 圆规 | 屏幕上可拖动、旋转的透明教具；笔尖沿边缘吸附 | **新增**（透明子窗口 + RotateTransform） |
| 函数作图 | 手写 y=f(x) 或公式识别 → 曲线插入墨迹层 | `MathGraph/` 三件套已有，补 UI 入口 |
| 坐标系 | 平面 / 空间直角坐标系 5 种 | `ShapeCoordinate1-5.svg` 已有 |
| 圆锥曲线 | 椭圆 / 双曲线 / 抛物线（带焦点版本） | 图形库已有 |
| 手写公式识别 | 公式 → MathML → 可编辑文本 | `InkRecognizeHelper` + `MathMLParser` 已有 |

数学老师预设额外默认：图形面板置顶、坐标系图形收藏置前。

---

## 5. 输入方式自适应

| 参数 | 大屏触摸 | 手写板+电脑 | 家用鼠标 |
|---|---|---|---|
| 检测方式 | 触摸 / 笔输入事件 | `Tablet.StylusDevices` 存在 | 两者皆无 |
| 按钮尺寸 | 48–56px | 36–40px | 32px |
| 按钮间距 | 12px | 8px | 6px |
| 工具提示 | 长按 600ms | 悬停 400ms | 悬停 300ms |
| 悬停态 | 禁用（红外屏无悬停） | 启用 | 启用 |
| 快捷键 | 隐藏（讲台无键盘） | 全量 | 全量 + 设置页展示 |
| 默认档位 | 首次向导决定 | 档位 1 | 档位 0 |
| 手掌抑制 | 强制开启 | 开启 | 关闭 |

- 设置 → 界面 → 「输入方式」：自动 / 触摸 / 鼠标（手动覆盖）
- 检测实现：`Tablet.TabletDevices.Count` + 挂 `InputManager.Current.PreProcessInput` 跟踪最近输入设备类型，**10 秒防抖**后才切换，避免混插设备来回跳

---

## 6. 视觉与组件规范（Phase A）

- **图标**：全量切换 `Resources/Icons/Fluent`（60+ 张已就位），废弃 MDL2 SymbolIcon / 位图混用
- **主题 token**：`Dark.xaml` / `Light.xaml` 从 5 个画刷扩充为完整 token 表
  - `FloatBarBackground / Foreground / BorderBrush`（已有）
  - `AccentSelected`（收编散落的 `#FF0088FF` 选中环）
  - `DangerBadge`（收编 `#E5484D` 角标）
  - `SurfaceConfirm`（收编清屏确认框的 White 底）
- **触控目标**：触摸模式 ≥48px；悬停 / 按下 / 选中三态反馈
- **弹层根治**：`ViewboxFloatingBar` 换成带 `LayoutTransform` 缩放的 Grid，让 WPF 真 `Popup` 可用——笔粗细 / 图形 / 调色板弹层不再依赖负 Margin 悬挂

---

## 7. 悬浮条信息架构（Phase B）

- 三档悬浮条统一按 **书写 · 修正 · 页面** 分组，分隔线划开
- 当前颜色胶囊常显在画笔按钮上，单击展开完整调色板（最近使用 + 自定义取色，保留 `Colors\*.ini` 绑定）
- **上下文工具菜单**（GoodNotes Active Tool Menu 模式）：选中画笔时在条上方弹颜色 + 粗细 + 荧光笔；选中图形时弹图形面板——替代分散的固定面板
- 撤销 / 重做紧邻工具区（希沃同款位置，教师最高频动作之一）
- 避坑：高频工具保持一键直达，不做 GoodNotes 2025-09 式的过度收纳

---

## 8. 白板模式布局（Phase C）

- **底部居中 Dock**：工具与翻页 / 页码合并（希沃同款位置），讲台两侧天然等距；撤掉现有四角重复的翻页按钮
- **页面缩略图侧栏**：多页板书快速跳转，可收起为细条
- **设置页翻新**：`SettingsPage.xaml` 用项目已引用的 iNKORE.UI.WPF.Modern 控件（ToggleSwitch、分区卡片）重排
- **旧 UI 退役**：`-o` 模式竖排工具栏确认无人使用后删除（`MW_Init.cs` 中折叠的那套）

---

## 9. 实施路线图

| 阶段 | 内容 | 改动面 | 风险 |
|---|---|---|---|
| Phase A | 视觉统一：Fluent 图标、token 补全、触控目标、三态 | 资源层 + XAML | 极低 |
| Phase B | 三档渐进式悬浮条 + 切换机制 + 首次向导 | XAML 重排 + 状态机代码 | 低 |
| Phase C | 白板模式底部 Dock + 多页缩略图 + 设置页翻新 | 新组件 | 中 |
| Phase D | 数学工具盘（虚拟教具新增、MathGraph 入口化）+ 输入自适应 | 新功能 | 中 |

每阶段独立发版；A + B 可合并为一个版本先行落地。

---

## 10. 技术实现要点（WPF）

- **档位状态机**：`MW_FloatBar.cs` 增加 `SetToolLevel(int)`，只切 `Visibility` 不销毁控件；配置持久化 key 带机器标识，实现"按设备记忆"
- **输入检测**：启动时 `Tablet.TabletDevices`；运行时 `InputManager.Current.PreProcessInput` 统计最近输入类型，防抖 10 秒
- **虚拟教具**：独立 `AllowsTransparency` 透明窗口；旋转 = `RotateTransform`；边缘吸附在 `StylusPlugIns` DynamicRenderer 层做命中偏移
- **首次向导**：`WelcomeWindow.xaml` 改三卡片单选，结果写入 Settings
- **动画**：档位切换用 `ScaleTransform` + `Opacity` 动画 150ms（悬浮条已有动画基建可复用）
- **向后兼容**：`-o` 旧 UI 与 `-b` 窗口模式保留；档位机制只作用于新 UI 路径

---

## 11. 参考产品与借鉴点

| 产品 | 借鉴 | 避坑 |
|---|---|---|
| Epic Pen | 极简批注哲学 "stays out of your way"；可收起工具条；免费版只给笔/荧光笔/橡皮/撤销 | — |
| ZoomIt | 快捷键驱动的极简交互（家用鼠标模式参考） | 大屏无键盘，不可照搬 |
| 希沃白板 5 | 底部常驻翻页/页码；学科工具齐全；快捷白板一键开 | 工具藏太深，新手找不到批注入口 |
| GoodNotes 6 | Active Tool Menu 上下文工具菜单 | 2025-09 过度收纳遭 459 票投诉，高频工具必须直达 |
| Microsoft Whiteboard | Fluent 圆角 Dock、悬停态 | — |

---

## 附：现状问题清单（v1 诊断）

1. **单排过载**：白板模式下悬浮条约 20 项平铺一排，书写工具、撤销/重做、模式开关、设置/重启/退出混排，无分组分隔
2. **触控目标偏小**：按钮 36px 级、图标 20×24，对 86″ 红外屏误触率高（Win11 触控规范建议 ≥44px）
3. **图标三套体系并存**：MDL2 SymbolIcon、自绘 Path、位图橡皮混用，Fluent 图标库 60+ 张已就位却未全量启用
4. **主题 token 匮乏**：Dark/Light 仅 5 个画刷，选中环、角标、确认框底色等硬编码色散落各处
5. **弹层脆弱**：Viewbox 内不能用 Popup，笔粗细 / 图形 / 快捷设置面板全靠负 Margin 悬挂，位置写死
